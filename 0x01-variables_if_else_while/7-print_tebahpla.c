#include <stdio.h>
/**
 * main - Prints the numbers from 0 to 9
 * Return: Always 0 (success)
 */

int main(void)
{
	char n;

	for (c = 'z'; n <= 'a'; c--)
	{
		putchar(c);

	}

	putchar('\n');
	return (0);

}
